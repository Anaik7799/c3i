# Dependencies
node_modules/
_build/
deps/
.elixir_ls/

# Compiled artifacts
bin/
*.so
*.o
*.plt
*.plt.hash
erl_crash.dump

# Rust build artifacts
target/

# Secret files
.env
.env.local

# Large files in .opencode
.opencode/bin/

# Prevent root clutter — generated/temp artifacts
/*.skill
/picked_files.txt

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml
sub-projects/work/gdrive/

# ML model caches (large blobs, not source)
sub-projects/scripts-gleam/.fastembed_cache/
**/.fastembed_cache/
**/huggingface/hub/

# Pass 8 followup: runtime artefacts that shouldn't be committed
.playwright-mcp/
.c3i-context.json
data/logs/
data/script-output/
data/smriti/Smriti.db
data/smriti/
docs/journal/monitor/
**/_apalache-out/

# Secrets — never commit (use .env / sops-nix)
devenv.nix
.devenv/

# Pass-31 hygiene — runtime/dev-env drift (SC-DRIFT-001..008)
# User shell + dev-env files (per-developer, not project state)
.envrc
.zshrc
.env.bak.*
.home/

# Pre-commit hook configuration (per-developer)
.pre-commit-config.yaml

# Runtime DBs and locks
# fy27-plan.db: zero-byte placeholder; real DB lives in sub-projects/work/gdrive/
fy27-plan.db
.claude/scheduled_tasks.lock
.claude/settings.json.bak.*
data/smriti/Smriti.db-shm
data/smriti/Smriti.db-wal

# Artifact-sync output listings (regenerated each daily-sync)
/claude_agents.txt
/claude_cmds.txt
/claude_rules.txt
/gemini_agents.txt
/gemini_cmds.txt
/gemini_rules.txt

# Pi runtime workspace
# Pass-31 hygiene: full .pi/ excluded — contains live API keys (config.json,
# config.example.json). Bring back into git only after secrets are extracted
# to .env / sops-nix and example file uses placeholder shape (SC-SEC-049).
.pi/

# Timestamped state snapshots (ephemeral)
states/

# Health monitor outputs (regenerated continuously)
docs/health/

# Runtime DB / RocksDB / vendored extracts
sub-projects/sutra/tuwunel/data/
sub-projects/sutra/sutra_server/
.pi/zenoh-ts-extract/
data/tmp/backup/
data/tls/selfsigned/

# Embedded git repos under sutra (each owns its own state)
sub-projects/sutra/element-x-ios/
sub-projects/sutra/matrix-rust-sdk/
sub-projects/sutra/fluffychat/

# Script output (regenerated each invocation)
sub-projects/scripts-gleam/data/script-output/

# Agda compiler interface files (regenerated by `agda --compile`)
*.agdai

# Pass-31 followup: more runtime tree drift
data/marionette-healthcheck.log
data/monitor/
data/sa-plan-logs/
data/smriti.db
data/smriti/
data/tla/
data/tls/

# Playwright runtime
tests/playwright/test-results/
tests/playwright/cross-browser-report.json
tests/playwright/preflight-report.json
tests/playwright/node_modules/
tests/playwright/package-lock.json

# Recursive nested checkout (broken/runtime)
sub-projects/sub-projects/

# Sutra runtime / nested test scaffolding
sub-projects/sutra/element_x_test/
sub-projects/sutra/matrix_client_test/.dart_tool/

# Nested git repos that are NOT (yet) registered submodules
# (treated as opaque worktrees — owned by their own .git)
sub-projects/c3i/
sub-projects/pi-mono/
sub-projects/openclaw/
sub-projects/marionette_mcp/

# Local work/build trees and one-off helper output
sub-projects/work/
sub-projects/ferriskey-vendored/
fix_gleam_warnings.py
lib/cepaf_gleam/build_warnings.log
lib/cepaf_gleam/test_output.log
data/logs/stop-hook-timing.log
data/logs/disk-trend.log
