@experience @color_rich @P0
Feature: Color Rich User Journeys & Fractal Path Exhaustion
  As an operator or symbiotic entity
  I need high-vibrancy, chromatic feedback across all 8x8 fractal matrix cells
  So that I can maintain absolute situational awareness and symbiotic homeostasis

  # =============================================================================
  # L0-L3: CORE METABOLISM & HOLON VISUALIZATION
  # =============================================================================

  @l0 @l1 @l2 @l3 @metabolism @P0
  Scenario: CR-PATH-001 - Real-time metabolic rate visualization
    Given the system is operating in "Color Rich" profile
    When the metabolic rate (Zenoh telemetry) increases to 85%
    Then the "Health Score" gauge should transition from "Deep Forest Green" to "Vibrant Indigo"
    And the background should pulsate with a frequency matching the OODA cycle
    And terminal elements (UI buttons) should increase in saturation

  @l3 @holon @P0
  Scenario: CR-PATH-002 - Holon symbiotic alignment feedback
    Given I am viewing the "Prajna Digital Twin"
    When the symbiotic alignment with the Naik-Genome exceeds 0.95
    Then the "Alignment Ribbon" should glow with "Electric Gold"
    And "Thinking" bubbles should appear for all active AI agents
    And the interface should prioritize "Founder's Directive" insights

  # =============================================================================
  # L4-L7: MESH, CLUSTER & FEDERATION JOURNEYS
  # =============================================================================

  @l4 @l5 @l6 @cluster @P0
  Scenario: CR-PATH-003 - Mesh node health matrix exhaustion
    Given I am auditing the 15-node mesh health matrix
    When I navigate through all terminal nodes in the "Node Detail" graph
    Then every node should represent its state with "Color Rich" gradients:
      | State       | Palette Range               |
      | Healthy     | Teal → Emerald              |
      | Scaling     | Sky Blue → Violet           |
      | Degraded    | Amber → Deep Orange         |
      | Critical    | Pure Red (#FF0000)          |
    And no "Dead End" elements should exist in the navigation flow

  @l7 @federation @P1
  Scenario: CR-PATH-004 - Cross-holon federation audit
    Given the holon is part of a global federation
    When I access the "Federation Portal" (L7)
    Then I should see a 3D graph representing cross-holon connections
    And edge colors should represent "Latency" and "Trust" scores
    And I should be able to drill down to L0 (Runtime) of any connected holon

  # =============================================================================
  # CONTROL & DATA FLOW EXHAUSTION
  # =============================================================================

  @control_flow @P0
  Scenario: CR-FLO-001 - Arm & Fire state machine verification
    Given I am attempting a destructive action (Mesh Apoptosis)
    When I select the action in the "Control Surface" (Zone D)
    Then the action must be "Armed" (Multi-step verification)
    And the button should glow "Danger Crimson"
    When I perform a "Sustained Hold" for 3 seconds
    Then the "Fire" signal should be published to Zenoh
    And the transition should be logged in the "Immutable Register"

  @data_flow @P0
  Scenario: CR-FLO-002 - Substrate-to-Surface data flow integrity
    Given a "Sentinel Threat" is detected at the substrate (Zenoh)
    When the signal propagates through the 8 layers of the fractal matrix
    Then it should appear in the WebUI in <100ms
    And it should carry the "Original ProofToken" from the Prometheus Verifier
    And the visual representation must match the "Color Rich" severity map
