# v21.3.0 SIL-6 Biomorphic Fractal Test Plan

**Version**: 1.0.0
**Date**: 2026-01-08
**Classification**: SIL-6 VERIFICATION MANDATE
**Scope**: 7-Layer Fractal Architecture (L0-L7)

## 1.0 Overview
This test plan defines the verification strategy for the **SIL-6 Homeostasis Mode** of the Indrajaal system. It employs a **Directed Telescope** approach, zooming in from the Federation (L7) down to the Runtime (L0), ensuring fractal self-similarity and biomorphic integrity at every scale.

## 2.0 7-Layer Fractal Verification Matrix

| Layer | Component | Test Scope | Verification Method |
|---|---|---|---|
| **L7** | **Federation** | Cross-holon attestation, Global invariants | Quint Formal Model Check |
| **L6** | **Cluster** | Quorum consensus (2oo3), Network partition | Chaos Engineering (Mara) |
| **L5** | **Node** | Runtime stability, BEAM scheduling | Health Check Probe (Zenoh) |
| **L4** | **Container** | Isolation, Resource limits, PID namespace | Container Inspection |
| **L3** | **Holon** | Agent logic, Biomorphic reflex, Immunity | BDD Scenarios (Elixir) |
| **L2** | **Component** | Module cohesion, Message passing | Integration Tests |
| **L1** | **Function** | Input/Output contracts, Type safety | Unit Tests (PropCheck) |
| **L0** | **Runtime** | Compilation, Boot sequence, NIF loading | Binary Verification |

## 3.0 Test Stages

### 3.1 Stage 1: Standalone (L0-L3)
**Goal**: Verify the atomic integrity of a single node.
- [ ] **L0**: Code compiles with 0 warnings (`mix compile --warnings-as-errors`).
- [ ] **L1**: `LineageAuth` NIF loads and verifies signature.
- [ ] **L2**: `ZenohPulse` emits heartbeat locally.
- [ ] **L3**: `OODALoop` cycles state (Observe -> Act).

### 3.2 Stage 2: Cluster Mode (L4-L6)
**Goal**: Verify local mesh consensus and container orchestration.
- [ ] **L4**: Podman containers (`app`, `db`, `obs`) start and pass healthchecks.
- [ ] **L5**: Nodes connect via `libcluster`.
- [ ] **L6**: `GlobalRegistrar` achieves quorum (2 nodes).

### 3.3 Stage 3: Full Mesh (L6-L7)
**Goal**: Verify federation and homeostasis.
- [ ] **L7**: Cross-holon state replication via `ImmutableState`.
- [ ] **Homeostasis**: System survives forced node death (Chaos Injection).
- [ ] **Biomorphic**: Immune system detects and neutralizes mock threat.

## 4.0 Sensory Channels Verification
The test plan explicitly verifies all sensory inputs:
1.  **Console**: Verbose ANSI logs (Linux-boot style).
2.  **Files**: `data/logs/sil6-homeostasis.log` (Durable record).
3.  **Telemetry**: `:telemetry` events bridge to `Indrajaal.Observability`.
4.  **Zenoh**: Real-time control plane messages on `indrajaal/control/**`.

## 5.0 Execution Protocol
1.  Run `dotnet fsi lib/cepaf/scripts/SIL6HomeostasisOrchestrator.fsx boot`
2.  Monitor console for 6-stage boot sequence.
3.  Verify "HOMEOSTASIS ACHIEVED" banner.
4.  Execute `dotnet fsi lib/cepaf/scripts/SIL6HomeostasisOrchestrator.fsx status`.
