\* Apalache config: canonical safety-only Spec.
\* For liveness + TLC, use SchedTele_Fair.cfg with `tlc -config SchedTele_Fair.cfg`.
SPECIFICATION Spec

CONSTANTS
    MaxJobs         = 2
    MaxAttempts     = 2
    TelemetryBufCap = 4
    TimeoutTicks    = 3
    HeartbeatTicks  = 2
    MaxTicks        = 6

INVARIANTS
    Inv_TypeOK
    Inv_NoDualTerminal
    Inv_AttemptBounded
    Inv_TelemetryBounded

CHECK_DEADLOCK
    FALSE
