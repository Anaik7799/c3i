\* TLC config for WorkerDispatch_BugCounterExample.tla — pre-Pass-10 broken model.
\* Expected result: TLC reports a counter-example for
\*   DispatcherRegistryConsistency in the INITIAL state (no Next steps needed).
CONSTANTS
    Workers       = {"a", "b", "c", "gleam_run"}
    DispatchSites = {"workers_rs", "scheduler_rs"}
    MaxJobs       = 4
INIT Init
NEXT Next
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
    DispatcherRegistryConsistency
    DispatcherSingularity
    NoUnknownWorkerSucceeds
