namespace Cepaf.KmsCatalog.Tests

open System
open NUnit.Framework
open Cepaf.KmsCatalog.Tests

// MASTER TEST SUITE ENTRY POINT
// Execute with: dotnet test --filter "Level"

module Program =
    let [<EntryPoint>] main args =
        // This is optional if using NUnit test runner, 
        // but useful for standalone debugging of the test suite.
        0
