# C3I Mission Orientation Prompt — UI & Substrate Control

**Version**: 22.0.0-GLM
**Status**: ACTIVE
**Objective**: This prompt defines the exact context, constraints, and execution requirements for developing, testing, or operating the C3I Biomorphic Mesh UI layers.

---

## 1. Environment & Architecture Context
You are working on the **Indrajaal C3I System**, a SIL-6 high-assurance distributed mesh.
*   **Substrate Layer (L1-L4)**: Powered by the Rust **Ignition Daemon** (`sa-up dashboard`). 
    - *Control*: `podman` CLI integration.
    - *Telemetry*: `ratatui` TUI with Zenoh 1.0 real-time OODA spans.
*   **Cognitive Layer (L5-L7)**: Powered by the **Gleam Penta-Stack**.
    - *Web*: Lustre MVU (Strictly **NO JAVASCRIPT** / SSR only).
    - *API*: Typed Wisp endpoints.
    - *Agentic*: AG-UI 32-Event Protocol + A2UI Declarative Security Catalog.

---

## 2. Operational Directives (The 10-Minute Mission)
When testing the dashboard or substrate, you MUST verify the following phases:
1.  **Synthetic Check**: Verify all 12 tabs with high-entropy mock data to prove layout stability across $W \in [40, 200]$ and $H \in [10, 60]$.
2.  **Wiring Verification**: Transition to real-time system data from `podman stats` and ensure the Zenoh data paths are operational.
3.  **Actual Operations**: Perform substrate mutations (START/STOP) on application containers.
    - *Controls*: `s` (Start), `x` (Stop).
    - *Log Validation*: Observe real-time OODA spans (`Observe`, `Orient`, `Decide`, `Act`) generated by these mutations.

---

## 3. Telemetry & AI Visibility Mandate
*   **Zenoh Integration**: Every state change MUST be published to the Zenoh mesh.
*   **OTel Spans**: Every control action MUST generate an `OtelSpan` with unique `TraceId` and `OODA` phase tags.
*   **AI Ingestion**: These messages are consumed by the Gemini/Claude AI agents via the MCP Zenoh subscription tool, enabling autonomous system monitoring and improvement without screen-scraping.

---

## 4. Verification & Testing Standards (Math Gates)
A change is only complete if it passes the **C1-C8 Gold Standard**:
*   **Entropy ($H \ge 2.5$)**: Verify the UI handles extreme state variations (0 nodes vs 100 nodes).
*   **Cyclomatic Completeness ($CCM \ge 0.90$)**: 100% path coverage of the UI update logic.
*   **Temporal Stability**: Run accelerated monitoring for 30s-60s per tab to prove zero memory leaks in scrolling buffers.
*   **Zero Warnings**: The build MUST produce zero compiler warnings (SC-GLM-CMP-001).

---

## 5. System Artifact Consensus
The following root files are the **Supreme Law**:
- `CLAUDE.md`: Gleam-first mandates and project shortcuts.
- `GEMINI.md`: Multi-language build order and safety constraints.
- `AGENTS.md`: Swarm hierarchy and responsibilities.
- `docs/GLEAM_UI_DEVELOPMENT_PROMPT.md`: Detailed coding guide.

---
**Execution**: State your Fractal Layer (L0-L7) and begin the Plan-Act-Validate loop. 
**Jidoka**: Stop immediately on any test failure or preflight flight-check error.
